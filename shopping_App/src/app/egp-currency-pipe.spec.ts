import { EgpCurrencyPipe } from './egp-currency-pipe';

describe('EgpCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new EgpCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
