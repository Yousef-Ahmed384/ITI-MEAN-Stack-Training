import { RatingcomponentPipe } from './ratingcomponent-pipe';

describe('RatingcomponentPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingcomponentPipe();
    expect(pipe).toBeTruthy();
  });
});
